fastapi==0.115.5          # Основний веб-фреймворк для створення REST API сервісу інференсу моделі
uvicorn[standard]==0.32.0 # ASGI-сервер для запуску FastAPI; забезпечує асинхронну обробку запитів
pydantic==2.9.2           # Валідація та типізація даних у FastAPI; моделі для вхідних/вихідних DTO
python-dotenv==1.0.1      # Завантаження конфігурацій/секретів з .env-файлів у середовище виконання

torch==2.8.0              # PyTorch: фреймворк для роботи з GNN/Transformer моделями та інференсу на CPU/GPU
numpy==1.26.4             # Базові числові обчислення, векторизація, підтримка тензорних операцій

boto3==1.35.20            # AWS SDK для Python: завантаження/збереження моделей у S3, доступ до AWS-сервісів

pytest==8.3.3             # Фреймворк для написання модульних та інтеграційних тестів

pandas==2.2.3             # Аналіз даних, робота з таблицями, передобробка вибірок, завантаження датасетів

plotly>=5                 # Побудова інфографіки та інтерактивних графіків для аналізу результатів моделі